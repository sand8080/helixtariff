#!/usr/bin/make -f
# -*- makefile -*-
# Sample debian/rules that uses debhelper.
# This file was originally written by Joey Hess and Craig Small.
# As a special exception, when this file is copied by dh-make into a
# dh-make output file, you may use that output file without restriction.
# This special exception was added by Craig Small in version 0.37 of dh-make.

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1


export FAKE_ROOT=$(CURDIR)/debian/python-helixtariff
export DESTDIR=$(FAKE_ROOT)/usr/share/python-support/python-helixtariff
export DESTDIR_SHARE=$(FAKE_ROOT)/usr/share/python-helixtariff

configure: configure-stamp
configure-stamp:
	dh_testdir
	# Add here commands to configure the package.
	touch configure-stamp


build: build-stamp

build-stamp: configure-stamp 
	dh_testdir
	
	# Add here commands to compile the package.
	#$(MAKE)
	#docbook-to-man debian/helixtariff.sgml > helixtariff.1
	#python src/helixtariff_tests.py
	
	touch $@

build-indep:
	dh_testdir

clean:
	dh_testdir
	dh_testroot
	rm -f build-stamp configure-stamp
	dh_clean

install: build
	dh_testdir
	dh_testroot
	dh_clean -k
	dh_installdirs
	cp -r src/helixtariff $(DESTDIR)
	find $(DESTDIR) -name '*pyc' -exec rm {} \;
	cp -r src/patches $(DESTDIR_SHARE)
	install src/helixtariff_install $(DESTDIR_SHARE)
	find $(DESTDIR_SHARE) -name '*pyc' -exec rm {} \;
	install src/helixtariff_run $(FAKE_ROOT)/usr/bin
	install -m 0644 supervisor-config/helixtariff.ini $(FAKE_ROOT)/etc/supervisor/process-available
	mv $(DESTDIR)/helixtariff/conf/settings.py $(DESTDIR)/helixtariff/conf/settings.py.tpl

# Build architecture-independent files here.
binary-indep: build install
	dh_testdir
	dh_testroot
	dh_installdebconf
	dh_installdocs
	dh_pysupport --noscripts
	dh_installchangelogs
	dh_link
	dh_strip
	dh_compress
	dh_fixperms
	dh_installdeb
	dh_gencontrol
	dh_md5sums
	dh_builddeb

binary: binary-indep
.PHONY: build clean binary-indep binary install configure
